fn fib(i:i32) i32 {
    if (i<=2) {
       ret 1
    }
    ret fib(i-1) + fib(i-2)
}

fn fib2(i:i32) i32 {
    prev := 0
    cur := 1
    
    for (j := 0; j < i; j++) {
        next := prev + cur
        prev = cur
        cur = next
    }
    
    ret prev;
}

fn main() {
    fib2(30)
}