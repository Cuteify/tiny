struct Person {
    pub name: string
    priv age: int
    prot address: string
}

struct Student : Person {
    pub grade: int
    pub score: float
}

// 测试空struct
struct Empty {
}

// 测试多种类型
struct Complex {
    pub id: int
    pub title: string
    pub isActive: bool
    pub rating: float
}

fn main() int {
    ret 0
}